[project]
name = "smalloldgames"
version = "0.1.0"
description = "Retro mobile game remakes in Python with a Vulkan renderer."
requires-python = ">=3.13"
dependencies = [
    "glfw>=2.10.0",
    "vulkan>=1.3.275.1",
]

[project.scripts]
smalloldgames = "smalloldgames.engine.app:main"
